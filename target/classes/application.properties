# ------------------------------------------------------------------------
# ---------------------TESTING/LOCAL------------------------
local.run.db=H2
# ---------------------PRODUCTION/DEPLOY------------------------
#local.run.db=POSTGRESQL
# ------------------------------------------------------------------------
#
# Configurations useful for working with H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true
#
# We set a port that is not frequently used
server.port=${PORT:2019}
#
# Feature that determines what happens when no accessors are found for a type
# (and there are no annotations to indicate it is meant to be serialized).
spring.jackson.serialization.fail-on-empty-beans=false
#
# keeps a transaction inside of the same entity manager
# This property register an EntityManager to the current thread,
# so you will have the same EntityManager until the web request is finished.
spring.jpa.open-in-view=true
#
# ------------------------------------------------------------------------
# ---------------------TESTING/LOCAL------------------------
spring.jpa.hibernate.ddl-auto=create
spring.datasource.initialization-mode=always
command.line.runner.enabled=true
# ---------------------PRODUCTION/DEPLOY------------------------
#spring.jpa.hibernate.ddl-auto=update
#spring.datasource.initialization-mode=never
#command.line.runner.enabled=false
# ------------------------------------------------------------------------
#
# Used to set the date format for JSON Output
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=America/Los_Angeles
#
# Turns off Spring Boot automatic exception handling
server.error.whitelabel.enabled=false
spring.mvc.throw-exception-if-no-handler-found=true
spring.resources.add-mappings=false
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dev-41327786.okta.com/oauth2/default
okta.oauth2.client-id=0oabbqmovHnQToRBP5d6
okta.oauth2.issuer=https://dev-41327786.okta.com/oauth2/default
SPRING.DATASOURCE.URL=jdbc:postgresql://localhost:5432/oktafoundationdb?password=password&sslmode=prefer&user=postgres